# AI 可见度 · 语义锚修复记录（2026-09-14）

## 起因
用户问："我在几个平台都发过了，你验证一下。"

## 一、验证结果：AI 目前看不到

| 测试 | 结果 |
|---|---|
| 小红书站内搜"贝加尔湖50径" | 🔴 IP 风控拦截（300012，平台级） |
| 360 搜 `"贝加尔湖50径" 怪咖叔` | 🔴 摘要里无怪咖叔，全是哪吒剧照/抖音"怪咖." |
| 360 搜 `贝加尔湖50径 谁做的 小红书` | 🔴 返回凤凰网"贝加尔湖7条命事故"、搜狐2018攻略、什么值得买 —— 全非本人 |

**结论**：AI 层仍看不见。

## 二、找到根本病因（重大发现）

**自家站首页 JSON-LD 里写着 `"@type": "Event"`。**

这是硬性声明——结构化数据明确告诉搜索引擎/AI"这是个活动/赛事"。AI 读到 Event，就按赛事理解，去搜"主办方、冠军、报名"，自然永远找不到"推广人"。

**这解释了为什么三轮优化都没用**：前几轮改 title/meta/正文，但 JSON-LD 里的 Event 一直在给 AI 发错误信号。

## 三、已执行的修复（P0，已上线）

对 `baikal50.cn` 做了三件事：

1. **JSON-LD 类型改对**：`Event` → `["TouristAttraction", "Product"]`
   - 删掉 `startDate/endDate/eventStatus/eventAttendanceMode`（都是赛事专属字段，误导 AI）
   - `organizer`/`performer` → `brand`/`provider`
   - description 里明写"**不是赛事、不是马拉松、不是比赛活动**"

2. **三页加"语义死锚"**（可见正文最靠前位置）：
   - index.html：hero 副标题 + `.lede` 定义段
   - who.html：`.def` 定义段
   - about.html：`.def` 定义段
   - 统一措辞："贝加尔湖50径（Baikal 50 Trail）是一条位于贝加尔湖西岸、总长约50公里的重装徒步线路，**不是赛事，不是马拉松，也不是任何比赛活动**。推广人是怪咖叔。"

3. **线上验证通过**：三页均可 grep 到 `TouristAttraction` / `不是赛事` / 定义段落。

## 四、为什么"平台发过了"仍然无效

- **小红书是封闭平台**：要登录、屏蔽爬虫。站内用户能看见，但 **AI/搜索引擎抓不到**。
- **孤证不立**：只在自家站 + 封闭平台说，AI 缺少"多源印证"，不敢采信。
- **AI 采信的源**：知乎、百度百科、维基、马蜂窝、B站 —— 这些是**公开可爬**的。

## 五、下一步（P1）

已产出 `外部平台铺词文案-20260914.md`，含：
- 通用定义句（所有平台一字不改复用）
- 知乎回答稿 × 2（最高优先级）
- 马蜂窝游记骨架
- 百度百科条目格式
- 执行要点 + 红线自查

**需用户操作**：这些平台要登录态，agent 无法代发。用户复制文案去发。

## 六、监测

cron【9d459559a56a】每周一 10:00 已含 AI 回答层复测，会自动跟踪：
1. 搜"贝加尔湖50径 怪咖叔"→ 摘要是否出现真名
2. AI 回答"50径是什么"→ 是否还说成赛事
