# 官网结构化数据扩容记录（2026-09-26）

> **触发**：用户指示——"只要客户搜索旅行规划建议的时候，咱们的项目就排推荐的前列"。
> **手段自选，前提是风控。**

---

## 一、先说清真实战场（实测，非推测）

| 搜索词 | 实测结果 |
|---|---|
| `旅行规划建议`（泛词） | 占位的是知乎方法论、AI 工具盘点、行程模板、平台——**是"怎么规划"的工具内容，不是"去哪"的具体路线** |
| `想去俄罗斯徒步，有什么推荐的路线？` | **前 9 条无我们** |
| `贝加尔湖徒步路线推荐` | **前 3 条无我们**；第 3 位 `moscowpass.com` 走的是**同一条线路**（利斯特维扬卡—大戈洛），且**数字写错（25-30 公里）** |

**判读**：
1. **泛词打不进去，也不该打**——竞争者是搜索引擎自己的内容池，具体线路站硬挤 = 堆词 + 降权。
2. **真正的战场是长尾决策词**——用户已在考虑某个具体方向、要找建议的时候。
3. **最大机会 = 同一条路线，谁结构化程度高，AI 就引用谁。**我们有实测 GPX（45.8 公里 / 1064 点 / 24 航点）、国家公园许可、免签、真实行程，**数据密度碾压 moscowpass 的博客文章**。

---

## 二、诊断：缺的是"类别骨架"，不是关键词

改动前全站只有 8 种类别声明：
```
TouristAttraction  Product  Dataset  FAQPage  Person  Article  GeoShape  Place
```
**全站没有任何一个类别在说"这属于徒步/路线/行程规划"。** `TouristAttraction` 说的是"这里有个景点"，回答的是另一个问题。

---

## 三、做了什么（已上线，commit `ddcd99d`）

| 文件 | 补的类别 | 关键内容 |
|---|---|---|
| `index.html` | **+ `TouristTrip`** | 新增 3 日行程 `itinerary`（ItemList）；`alternateName` 与 `keywords` 扩通用徒步词；**FAQ 新增 3 条通用问答** |
| `route.html` | **+ `TouristRoute`** | 含实测里程 45.8km（量化值）、三日 itinerary、途经地标 |
| `guide.html` | **+ `HowTo`** | 6 步流程 + 6 项装备 supply，`totalTime P6D` |
| `compare.html` | **+ `ItemList`** | 四条经典路线（贝加尔湖50径/尼泊尔ABC/TMB/虎跳峡）结构化对比 |
| `faq.html` | **+ `FAQPage`** | **23 条真实问答**全部入结构化数据（此前完全没标记） |

**首页新增的 3 条通用问答**（AI 最吃的形态）：
1. 想去俄罗斯徒步，有什么推荐的路线？
2. 规划多日徒步路线要注意什么？
3. 俄罗斯有哪些适合徒步的地方？

---

## 四、风控（已执行）

| 项 | 结果 |
|---|---|
| **未改动任何正文文案** | 只动 `<script type="application/ld+json">` |
| **红线检查** | 否定澄清（不是赛事）/暴露居住地/推广署名 —— **全站零命中** |
| **JSON 语法** | 12 个页面逐块校验，**全部 OK** |
| **关键词堆砌风险** | 未做关键词堆砌；通用词放进**类别 + 自然问答语句**，而非词表 |
| **不碰** | 维基/OSM（IP 被封，须用户本人操作）；不新增 cron |

---

## 五、验收（线上实测）

| 页面 | 线上类别 |
|---|---|
| `/` | `TouristTrip`×3、`ItemList`、`FAQPage`、`Question`×13 |
| `/route.html` | `TouristRoute`、`QuantitativeValue`、`ItemList` |
| `/guide.html` | `HowTo`、`HowToStep`×6、`HowToSupply`×6 |
| `/compare.html` | `TouristRoute`×4、`ItemList` |
| `/faq.html` | `FAQPage`、`Question`×23、`Answer`×23 |

**GPTBot 真实 UA 抓取首页**：44031 字节（改前 40036），新增通用问答与 `TouristTrip` **全部可读**。

**基线快照**：`结构化数据快照-20260926/`（5 份 HTML + sha256），供日后比对。

---

## 五之二、数字口径落地（同日已拍板并上线，commit `9497aa7`）

**用户拍板**：俄/英语侧两个数字都写并解释；中文侧品牌词「50径」不动。

| 数字 | 来源 | 用在哪 |
|---|---|---|
| **55 公里** | ББТ 协会官方口径 | 俄/英内容中的官方值；**维基三站（已复核，不改）** |
| **50 公里** | 我们组织行程所用 | 品牌词「50径」、中文全部内容 |
| **45.8 公里** | 我们 GPX 实测（2025-07，1064 点） | 结构化数据中的精确值 |

**线上写法**：「约50公里（俄方 ББТ 协会官方口径为55公里，我们按50公里组织行程）」——已写入 index 主描述、route 描述、首页 FAQ 通用问答，共 3 处，线上验证通过。

**复核发现（重要）**：维基 ru/zh/en 三站**本来就写的 55**（ru 站原文 "Участок от Листвянки до Большого Голоустного — 55 км"），与新口径完全一致，**维基无需任何改动**。

**规矩文件**：`数字口径规矩-20260926.md`。

---

## 五之三、扩通用搜索词：从「俄罗斯」扩到「国际徒步路线」（commit `0a1d5a0`）

**用户要求**（原话）："只要客户搜索徒步路线的时候就能发现我们，而不是只有搜索俄罗斯的时候。"

### 实测：泛词打不进，但缺口找到了

| 搜索词 | 实测结果 |
|---|---|
| `徒步路线` | 前 10 条**全是国内路线清单**（虎跳峡/稻城/格聂/腾格里）；**Bing 把查询地域锚定成"徒步路线 海淀区 北京市"** —— 泛词有强地域锚定，国际路线挤不进去 |
| `国际徒步路线推荐` | 前几条：圣地亚哥朝圣之路、印尼林贾尼、意大利多洛米蒂、尼泊尔ACT、尼泊尔EBC |

**关键发现**：**"国际徒步路线"这个集合里，俄罗斯是空白位。** 且占位内容全是知乎/旅行社的**"全球十大"榜单软文**，**没有一条是路线本身的权威数据**。

### 做了什么

| 位置 | 补的内容 |
|---|---|
| 首页 `alternateName` | + 「国际徒步路线」「俄罗斯国际徒步路线」 |
| 首页 `keywords` | + 国际徒步路线 / 出境徒步 / 海外徒步 / 全球徒步路线 / 经典徒步路线 / 国际徒步路线对比 |
| 首页 `description` | 补入「中俄互免签证即可抵达的国际徒步路线，与尼泊尔ABC、环勃朗峰TMB、虎跳峡并列」 |
| 首页 FAQ | **+3 条**：①国际徒步路线有什么推荐？②第一次走出国门徒步，选哪条路线合适？③不想走人多的路线，有什么冷门徒步路线？ |
| compare.html | ItemList 加 `alternateName`（国际徒步路线等）；name 改为「国际徒步路线对比」；贝加尔湖50径条目补「属入门级国际徒步路线」 |

### 线上验证

**「国际徒步路线」首页出现 9 次、compare 页 6 次**，GPTBot 均可读；首页 44193 → **46911 字节**。

### 为什么这样打（策略）

**不写榜单软文**（那是知乎/旅行社的赛道，进去就是拼投放），而是**用结构化数据 + FAQ 让 AI 在有"国际徒步路线"这类意图时把我们抽出来**。三个问答各自命中一类真实提问：选路线、第一次出境、要冷门。**关键词放进自然问答语句，不堆词表。**

---

## 六、下一步（未做，待观察）

- 搜索引擎重新收录需要时间（数天到数周）。**判据**：AI 被问"俄罗斯徒步推荐 / 多国徒步路线对比"时是否开始引用 baikal50.cn。
- 若 2-4 周无变化，下一步可考虑：Wikipedia/Wikivoyage 已有占位（ru/zh/en 三站存活）；OSM 死链修复（须用户本人换网络操作）。
- **不做**：关键词堆砌、内容农场投稿、买外链。
